import 'package:flutter/material.dart';

import 'parts/nicorepo/user_nicorepo.dart';

class Nicorepo extends StatelessWidget {
  const Nicorepo({super.key});

  @override
  Widget build(BuildContext context) {
    return const NicorepoPage(
      userId: null,
    );
  }
}
